create a password locker account where an interested user will:
a) Create a new locker account. On creation, the app will:
    > Capture the following user details;
        1) first_name 
        2) last_name
        3) username and 
        4) passord
    > Check if a username is unique
b) Able to login. On login, the app will:
    > authenticate user login credentials (validated username and password)
    > login user if authenticated

c) Create/add existing account credentials. On creation/add, the app will:
    > capture the credent details;
        1) user_id (be provided after successful login)
        2) site name
        3) username
        4) password
            > give option to autogenerate or
            > user enters his/her password
d) View a list of his/her credentials. Details to include:
    > site name
    > username and 
    > password
e) Be able to remove/delete credentials that he/she no longer needs

